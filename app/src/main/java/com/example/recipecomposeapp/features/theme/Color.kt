package com.example.recipecomposeapp.features.theme

import androidx.compose.ui.graphics.Color

// Main accent colors
val PrimaryColor = Color(0xFF5F3678) // Purple for headers and accents
val AccentColor = Color(0xFFE13E3E) // Red for favorite button
val AccentBlue = Color(0xFF525DC0) // Blue for category buttons and slider

// Slider
val SliderTrackColor = Color(0xFFA1A9F9) // Light blue for slider track

// Backgrounds
val BackgroundColor = Color(0xFFF4FAFF) // Light blue app background
val SurfaceColor = Color(0xFFFFFFFF) // White for cards
val SurfaceVariantColor = Color(0xFF303030) // Dark gray for inactive elements
val DividerColor = Color(0xFFF5F5F5) // Light gray for dividers

// Text
val TextPrimaryColor = Color(0xFF000000) // Main text
val TextSecondaryColor = Color(0xFF666666) // Secondary text

// Main accent colors (adapted for dark theme)
val PrimaryColorDark = Color(0xFF9A9EFF) // Purple for headers and accents
val AccentColorDark = Color(0xFFE57373) // Red for favorite button
val AccentBlueDark = Color(0xFF64B5F6) // Blue for category buttons and slider

// Slider (dark theme)
val SliderTrackColorDark = Color(0xFF7986CB) // Light blue for slider track

// Backgrounds (dark theme)
val BackgroundColorDark = Color(0xFF121212) // Dark app background
val SurfaceColorDark = Color(0xFF1E1E1E) // Dark gray for cards
val SurfaceVariantColorDark = Color(0xFF424242) // Gray for inactive elements
val DividerColorDark = Color(0xFF2C2C2C) // Dark gray for dividers

// Text (dark theme)
val TextPrimaryColorDark = Color(0xFFE6E6E6) // Main text
val TextSecondaryColorDark = Color(0xFFB0B0B0) // Secondary text
